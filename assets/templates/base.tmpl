{{define "base"}}
<!doctype html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
        <title>storemypdf - {{template "page:title" .}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        {{block "page:meta" .}}{{end}}
        
        <link rel='stylesheet' href='/static/css/tailwind.css?version={{.Version}}'>
    </head>
    <body>
        {{template "partial:navbar" .}}
        <main>
            {{template "page:main" .}}
        </main>
        {{template "partial:footer" .}}
    </body>
</html>
{{end}}
